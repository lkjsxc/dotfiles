# dotfiles

This repository contains NixOS configuration and related files for the system.

- Purpose: Provide system configuration for a KDE Plasma setup with a dark theme.
- Structure: See `docs/README.md` for details.

Quick actions:
- To apply changes: run `sudo nixos-rebuild switch --flake .#your-hostname` (adjust as needed).

